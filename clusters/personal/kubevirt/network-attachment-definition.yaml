apiVersion: k8s.cni.cncf.io/v1
kind: NetworkAttachmentDefinition
metadata:
  name: br0
  namespace: default
spec:
  config: |
    {
      "cniVersion": "0.3.1",
      "name": "br0",
      "plugins": [
        { "type": "bridge", "bridge": "br0", "ipam": {} },
        { "type": "tuning" }
      ]
    }
---
apiVersion: k8s.cni.cncf.io/v1
kind: NetworkAttachmentDefinition
metadata:
  name: br0
  namespace: kubevirt
spec:
  config: |
    {
      "cniVersion": "0.3.1",
      "name": "br0",
      "plugins": [
        { "type": "bridge", "bridge": "br0", "ipam": {} },
        { "type": "tuning" }
      ]
    }
